on:
  push:
  workflow_dispatch:


jobs:

  build:
    strategy:
      matrix:
        arch: [x64]
      fail-fast: false


    runs-on: windows-latest


    steps:

    - uses: fu1ny2t-3es/sy1yg2@build
      with:
        branch: upstream
        arch: ${{ matrix.arch }}

        token: ${{ secrets.token }}
