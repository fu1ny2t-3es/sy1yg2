on:
  workflow_dispatch:
  schedule:
    - cron: '45 18 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/sy1yg2@cron
        with:
          token: ${{ secrets.token }}
